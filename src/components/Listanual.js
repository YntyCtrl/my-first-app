const Listnual = ({ items }) => {

    let p = []
    let list = items.map(item => {
        for (list of items) {
            return <li>{item}</li>
        }

        
    })
    return <ol>{list}</ol>
}

export default Listnual;